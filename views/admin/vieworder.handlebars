<!DOCTYPE html>
<html>
<head>
  <title>Order View</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.6.1/font/bootstrap-icons.css">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    .product-card {
      display: flex;
      align-items: center;
      padding: 10px;
    }
    
   
  </style>
</head>
<body >
  {{>adminHeader}}
  <div style="height: 14vh;"></div>
  {{!-- <div class="container">
    <h1>Order View</h1>
    <div class="row">
      {{#each orderDetails}}
        <div class="col-md-4">
          <div class="card product-card">
            <img class="card-img-top" src="/public/images/products/{{this.image}}" alt="Product Image">
            <div class="card-body">
              <h5 class="card-title">{{this.name}}</h5>
              <p class="card-text">
                Quantity: {{this.quantity}} <br>
                Size: {{this.size}} <br>
                Price: {{this.price}} <br>
              </p>
              <a href="#" class="btn btn-primary">Edit Status</a>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  </div> --}}
  <section class="gradient-custom mt-5">
    <div class="container py-5 ">
      <div class="row d-flex justify-content-center align-items-center ">
        <div class="col-lg-10 col-xl-8">
          <div class="card" style="border-radius: 10px;">
            <div class="card-body p-4">
              {{#each orderDetails }}
              <div class="card shadow-0 border mb-4">
                <div class="card-body">
                  <div class="row">
                    <div class="col-md-3">
                      <img src="/public/images/products/{{this.image}}" class="img-fluid" alt="">
                    </div>
                    <div class="col-md-2 text-center d-flex justify-content-center my-2 align-items-center">
                      <p class="mb-0">{{this.name}}</p>
                    </div>
                    <div class="col-md-2 text-center d-flex justify-content-center my-2 align-items-center">
                      <p class=" mb-0 ">{{this.size}}</p>
                    </div>
                    <div class="col-md-2 text-center d-flex justify-content-center my-2 align-items-center">
                      <p class=" mb-0 ">Qty: {{this.quantity}}</p>
                    </div>
                    <div class="col-md-2 text-center d-flex justify-content-center my-2 align-items-center">
                      <p class=" mb-0 ">₹{{this.price}}</p>
                    </div>
                  </div>
                </div>
              </div>
              {{/each}}
            </div>
            <div >
            {{!-- <div >
              <p style="margin-left: 3vw;">Name:<span style="margin-left: 2vw;" >{{name}}</span></p>
              <p style="margin-left: 3vw;">phone:<span style="margin-left: 1.5vw;"></span>{{phone}}</span></p>
              <p style="margin-left: 3vw;">Email:<span style="margin-left: 1.7vw;"></span>{{email}}</span></p>
              <p style="margin-left: 3vw;">Address:<span style="margin-left: 0.5vw;">{{address.houseName}},{{address.landmark}},{{address.location}},<br>{{address.pincode}},{{address.district}},{{address.state}},{{address.countries}}</span></p>
              <p style="margin-left: 3vw;">Address type:<span ></span>{{address.type}}</span></p>

            </div> --}}
            <div style="margin-left: 4vw;" class="mb-5">
              <h3>Details</h3>
              <table>
                <tbody>
                  <tr>
                    <td>Name:</td>
                    <td>{{name}}</td>
                  </tr>
                  <tr>
                    <td>Phone:</td>
                    <td>{{phone}}</td>
                  </tr>
                  <tr>
                    <td>Email:</td>
                    <td>{{email}}</td>
                  </tr>
                  <tr>
                    <td>Address:</td>
                    <td>{{address.houseName}},{{address.landmark}},{{address.location}},<br>{{address.pincode}},{{address.district}},{{address.state}},{{address.countries}}</td>
                  </tr>
                  <tr>
                    <td>date of order:</td>
                    <td>{{formatDate time}}</td>
                  </tr>
                   <tr>
                    {{#if delivered}}
                    <td>date of delivery:</td>
                    <td>{{formatDate delivered}}</td>
                  </tr>
                  {{/if}}
                 
                </tbody>
              </table>
            </div>
            <div class="card-footer border-0 px-4 py-3"
              style="background-color: #2b2929; border-bottom-left-radius: 10px; border-bottom-right-radius: 10px;">
              <h5 class="d-flex align-items-center justify-content-end text-white text-uppercase mb-0">Total: <span
                  class="h3 mb-0 ms-2">{{total}}</span></h5>
            </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
  {{>adminfooter}}
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
